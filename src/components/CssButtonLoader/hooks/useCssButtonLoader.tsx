import { UseCssButtonLoaderProps } from '../cssButtonLoader.types';

export const useCssButtonLoader = ({ exampleProp }: UseCssButtonLoaderProps) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
