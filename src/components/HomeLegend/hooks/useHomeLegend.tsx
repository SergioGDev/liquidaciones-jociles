import { UseHomeLegendProps } from '../homeLegend.types';

export const useHomeLegend = ({ exampleProp }: UseHomeLegendProps) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
