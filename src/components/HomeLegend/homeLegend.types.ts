// Props for the component
export type HomeLegendProps = {
    // Put here the HomeLegendProps. For example:
    exampleProp: string;
};

// Props for the hook
export type UseHomeLegendProp = {
    // Put here the UseHomeLegendProp. For example:
    exampleProp: string;
};
