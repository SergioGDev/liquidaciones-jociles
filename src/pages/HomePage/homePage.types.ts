// Props for the component
export type HomePageProps = {
    // Put here the HomePageProps. For example:
    exampleProp: string;
};

// Props for the hook
export type UseHomePageProp = {
    // Put here the UseHomePageProp. For example:
    exampleProp: string;
};
