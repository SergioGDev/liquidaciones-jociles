export type {{pascalCase name}}ContextType = {
    // Add here the vars of the context. For example:
    // itemList: ItemType[];
};

export type {{pascalCase name}}ContextProps = {{pascalCase name}}ContextType & {
    // Insert here the functions to modify state. For example:
    // addItem: (item: ItemType) => void;
    // removeItem: (id: string) => void;
};

// Add here the common context's types. For example:

// export type ItemType = {
//     id: string;
//     name: string;
// }